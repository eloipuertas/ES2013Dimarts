using UnityEngine;
using System.Collections;

public class boss_fire_collider : MonoBehaviour {
	// Use this for initialization
	void Start () {
		Destroy(this.gameObject,30.0f);
	}
	// Update is called once per frame
	void Update () {
	}
}
